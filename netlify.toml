[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Scheduled cron job: auto-cancel no-show appointments every hour
# Mirrors the Vercel "0 * * * *" schedule
[[functions]]
  name = "auto-cancel-cron"
  schedule = "0 * * * *"
